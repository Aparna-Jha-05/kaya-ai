"use client";

import { useEffect, useState } from "react";
import { procurementApi, type SimulationResponse } from "@/lib/api";
import { recommendationLabel } from "@/lib/recordUtils";

interface TCOSliderProps { baseCapexCr: number; onTCOChange?: (tcoCr: number) => void; }
export default function TCOSlider({ baseCapexCr, onTCOChange }: TCOSliderProps) {
  const [discountPercent, setDiscountPercent] = useState(0);
  const [delayDays, setDelayDays] = useState(12);
  const [result, setResult] = useState<SimulationResponse | null>(null);
  const [state, setState] = useState<"calculating" | "ready" | "error">("calculating");
  useEffect(() => { let active = true; const timer = window.setTimeout(() => { setState("calculating"); procurementApi.simulate({ base_capex_inr: baseCapexCr * 10_000_000, discount_percent: discountPercent, delay_days: delayDays }).then((next) => { if (!active) return; setResult(next); setState("ready"); onTCOChange?.(next.calculated_tco2_inr / 10_000_000); }).catch(() => active && setState("error")); }, 180); return () => { active = false; window.clearTimeout(timer); }; }, [baseCapexCr, delayDays, discountPercent, onTCOChange]);
  const rejected = result?.recommendation === "REJECT";
  return <div className="rounded-xl border border-[#1e293b] bg-[#111827] p-5"><div className="mb-3 flex items-center justify-between"><div><p className="font-mono text-[11px] uppercase tracking-widest text-[#38bdf8]">Cost scenario</p><h3 className="text-base font-bold">5-year risk-adjusted cost (TCO²)</h3></div>{result && <span className={`rounded border px-2.5 py-1 text-xs font-bold uppercase tracking-wider ${rejected ? "border-[#f43f5e] bg-[#f43f5e]/15 text-[#f43f5e]" : "border-[#38bdf8] bg-[#38bdf8]/15 text-[#38bdf8]"}`}>{recommendationLabel(result.recommendation)}</span>}</div><p className="mb-4 text-xs text-[#94a3b8]">Adjust bounded commercial assumptions. The calculation service returns the result; it does not approve a purchase order.</p><div className="mb-4 space-y-4 rounded-lg border border-[#1e293b] bg-[#0b1220] p-4"><label className="block"><span className="mb-1 flex justify-between text-xs font-medium"><span>Upfront discount: <strong className="text-[#38bdf8]">{discountPercent}%</strong></span><span className="text-[#94a3b8]">Base: ₹{baseCapexCr.toFixed(2)} Cr</span></span><input aria-label="Capex discount percentage" type="range" min="0" max="25" value={discountPercent} onChange={(event) => setDiscountPercent(Number(event.target.value))} className="h-1.5 w-full cursor-pointer appearance-none rounded-lg bg-[#1e293b] accent-[#38bdf8]" /></label><label className="block"><span className="mb-1 flex justify-between text-xs font-medium"><span>Delivery delay: <strong className="text-[#fbbf24]">{delayDays} days</strong></span><span className="text-[#94a3b8]">Penalty: ₹2.0L/day</span></span><input aria-label="Delivery delay in days" type="range" min="0" max="30" value={delayDays} onChange={(event) => setDelayDays(Number(event.target.value))} className="h-1.5 w-full cursor-pointer appearance-none rounded-lg bg-[#1e293b] accent-[#fbbf24]" /></label></div><div aria-live="polite" className="rounded-lg border border-[#1e293b] bg-[#0b1220] p-3.5 text-sm">{state === "calculating" ? <p className="text-[#94a3b8]">Recalculating with the scenario service…</p> : state === "error" ? <p className="text-[#fbbf24]">Scenario service is unavailable. No replacement estimate is shown.</p> : <div className="grid grid-cols-3 gap-2 text-center"><Metric label="Upfront cost" value={`₹${(result!.adjusted_capex_inr / 10_000_000).toFixed(2)} Cr`} /><Metric label="Delay cost" value={`₹${(result!.delay_penalty_inr / 10_000_000).toFixed(2)} Cr`} /><Metric label="5-year cost" value={`₹${(result!.calculated_tco2_inr / 10_000_000).toFixed(2)} Cr`} /></div>}</div></div>;
}
function Metric({ label, value }: { label: string; value: string }) { return <div><p className="text-[10px] uppercase text-[#94a3b8]">{label}</p><p className="mt-0.5 font-mono font-bold text-white">{value}</p></div>; }
