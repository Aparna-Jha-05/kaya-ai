# Core backend
PO_LICE_ALLOWED_ORIGINS=http://localhost:3000
PO_LICE_PROJECT_ID=PRJ-AMBER-01
PO_LICE_DATA_DIR=backend/data
DEMO_MODE=true
SUPABASE_DATABASE_URL=
DB_POOL_MIN_SIZE=2
DB_POOL_MAX_SIZE=10
DB_REQUEST_TIMEOUT_SECONDS=10

# Local model extraction is disabled unless explicitly enabled.
OLLAMA_ENABLED=false
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=
ML_EXTRACTION_DEADLINE_SECONDS=20

# Remote extraction requires all four values below. Keep keys server-side.
REMOTE_EXTRACTION_ENABLED=false
REMOTE_EXTRACTION_PROJECTS=
GEMINI_MODEL=
GEMINI_API_KEY=
